@page "/Account/Manage/PersonalData"

@inject IdentityUserAccessor UserAccessor

<PageTitle>Dados Pessoais</PageTitle>

<StatusMessage />
<h3>Dados Pessoais</h3>

<div class="row">
    <div class="col-md-6">
        <p>Sua conta contém dados pessoais e essa página te permite deletá-los permanentemente</p>
        <p>
            <strong>Deletar esses dados será um processo permanente e você não poderá recuperá-los.</strong>
        </p>
        <p>
            <a href="Account/Manage/DeletePersonalData" class="btn btn-danger">Deletar</a>
        </p>
    </div>
</div>

@code {
    [CascadingParameter]
    private HttpContext HttpContext { get; set; } = default!;

    protected override async Task OnInitializedAsync()
    {
        _ = await UserAccessor.GetRequiredUserAsync(HttpContext);
    }
}
